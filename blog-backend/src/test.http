### 1. 获取所有文章列表
# 访问这个接口，应该能看到你数据库里所有的 JSON 数据
GET http://localhost:4080/articles
Accept: application/json

###

### 2. 发布一篇新文章
# 注意：POST 下方必须留一个空行，然后才是 JSON 内容
POST http://localhost:4080/articles
Content-Type: application/json

{
  "title": "我的第二篇博客",
  "content": "这是通过 IDEA 的 test.http 脚本发布的，后端三层架构跑通啦！"
}

###

### 3. 根据 ID 获取单篇文章详情
# 假设你刚刚发布的文章 ID 是 2，就访问这个地址
GET http://localhost:4080/articles/1
Accept: application/json