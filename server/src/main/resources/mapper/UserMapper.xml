<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="moon.yukiss.mapper.UserMapper">
    <select id="findById" resultType="moon.yukiss.entity.User">
        SELECT u.*,
               (SELECT COUNT(*) FROM user_follow uf WHERE uf.following_id = u.id) AS followerCount,
               (SELECT COUNT(*) FROM user_follow uf WHERE uf.follower_id = u.id) AS followingCount
        FROM user u
        WHERE u.id = #{id} AND u.is_deleted = 0
    </select>
</mapper>
