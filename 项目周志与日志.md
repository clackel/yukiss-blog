# Yukiss Blog 项目周志与开发日志

## 周志一：博客项目基础功能搭建

本周主要完成了 Yukiss Blog 项目的基础框架和用户模块开发。前端使用 Vue 搭建页面，通过路由实现首页、登录注册、文章详情和个人中心等页面之间的跳转；后端使用 Spring Boot 编写接口，并连接 MySQL 数据库保存用户及文章数据。在用户模块中，我实现了注册、登录和个人信息查询功能，同时使用 JWT 保存登录状态，并通过拦截器判断用户是否已经登录。开发过程中，我进一步熟悉了前后端分离项目的请求流程，也理解了 `@RequestBody`、`@RequestParam` 等注解在接收不同请求参数时的作用。经过接口联调，用户能够正常注册、登录并进入系统，为后续功能开发打下了基础。

## 周志二：博客互动功能完善

本周在已有用户功能的基础上，重点完善了文章发布、评论、点赞和个人资料管理等功能。用户登录后可以发布文章、查看自己的文章，也可以进入文章详情页发表评论或回复其他用户。文章和评论均增加了点赞功能，后端会根据当前用户判断是新增点赞还是取消点赞，并将最新点赞数量返回给前端。此外，我还完成了头像上传、邮箱绑定、密码修改和账号注销等个人中心功能，并对上传文件的格式及大小进行了限制。通过本周开发，我对 Controller、Service、Mapper 各层之间的职责有了更清楚的认识，也积累了接口设计、异常处理和前后端联调方面的经验。

## 开发日志一：完成前后端项目框架

今天主要搭建博客项目的整体结构。前端采用 Vue 和 Vite，完成了基础页面、公共样式以及页面路由配置；后端创建 Spring Boot 项目，并配置 MySQL、MyBatis 和服务器端口。随后设计用户与文章相关的数据结构，为后续接口开发做好准备。通过这次搭建，我对前后端分离项目的目录结构和基本运行流程有了更完整的认识。

## 开发日志二：实现用户注册与登录

今天集中开发用户模块，实现了注册、登录和当前用户信息查询接口。注册时会检查用户名格式及重复情况，并对用户密码进行加密保存；登录成功后，后端生成 JWT 返回给前端。前端会在后续请求的 `Authorization` 请求头中携带该令牌，后端拦截器负责验证身份。完成联调后，用户登录状态能够正常保存和识别。

## 开发日志三：实现文章、评论与点赞

今天完成博客的主要互动功能。用户可以发布文章、查看文章列表和文章详情，也可以在详情页发表评论或回复已有评论。文章和评论的点赞接口采用状态切换方式：未点赞时新增记录，已经点赞时取消记录。后端同时统计点赞数和评论数，并将当前用户的点赞状态返回给前端，使页面能够及时更新相应的显示效果。

## 开发日志四：完善个人中心和文件上传

今天主要完善个人中心功能，包括修改昵称与简介、更新密码、绑定邮箱、找回账号和注销账号。同时增加了头像上传接口，只允许上传 JPG、PNG、GIF 或 WebP 图片，并限制文件大小，上传成功后返回可访问的图片地址。开发过程中还补充了统一响应结构和全局异常处理，使前端能够根据响应信息向用户展示清晰的成功或错误提示。
